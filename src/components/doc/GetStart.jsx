export const GetStart = () => {
    return(
        <div>
            <span className="heading">Getting Started</span><br/>
            <span className="sub-heading">Signup and Login</span>
            <p>To get started, you must register a SIGMA API-Key and sign in to the SIGMA API-Key Developer Portal. 
                Upon logging in, go to the My Apps tab to start creating your first app.</p>
            <span className="sub-heading">Obtaining an API-Key</span>
            <p>Click the Sign up to get your first API-Key button to start the app creation process. 
                After a one-time survey about intended usage, the API-Key creation dialog will appear.
                Give your application a name and a simple description to register an API-Key. Each application must have its own unique API-Key.</p>
        </div>
    );
};